# convertX2Xへの機能追加・変更

convertX2Xは、変換機能ごとに利用できるAzure Functionsプロジェクトのコレクションです。
変更するときは、対象機能のREADMEを読み、その機能の手順で動作を確認してください。

初めて作業する場合は [開発者マニュアル](docs/README.md) から進めてください。[共通の作業手順](docs/development.md) と、[Excel](docs/excel2md.md) / [PowerPoint・PDF](docs/ppt-pdf-to-images.md) の実装・変更箇所を案内しています。

## 機能の置き場所

- 独立した入出力や実行環境を持つ、まとまった変換機能を `functions/<変換名>/` に配置します。
- 各ディレクトリを、単独でビルド・テスト・デプロイできるプロジェクトにします。
- 言語、ライブラリ、ビルドツールは機能ごとに選べます。全機能をJavaに揃える必要はありません。
- 依存関係、実行環境、起動手順は機能内で管理します。
- 別の機能をビルドしたり起動したりしないと使えない構成は避けます。
- 既存の文書画像変換は `functions/ppt-pdf-to-images/` にあります。
- ExcelからMarkdownへの変換は `functions/excel2md/` にあり、`ExcelMarkdownService` をHTTP・Queueから共有します。
- PPT・PPTX・PDFは、この1つのプロジェクト内で入力形式別のクラスとして実装します。
- 提供予定だけのディレクトリや、変換を行わないstubは追加しません。

ルートREADMEは機能のカタログです。実装と検証が済んだ機能だけを追加し、
対応形式と機能READMEへのリンクを載せます。詳細な設定は機能READMEで説明します。

## 機能READMEに書くこと

そのディレクトリだけを見て、利用者が起動から結果の取得まで進めるようにします。

| 項目 | 記載内容 |
| --- | --- |
| 対応形式 | 入力形式、出力形式、対応しない形式や条件 |
| 入力・出力 | リクエスト例、結果の形式、ファイル名、エラーの扱い |
| 呼び出し方法 | HTTP・Queueなど対応するトリガーと利用手順 |
| 実行手順 | 必要なツール、ビルド、ローカル起動、デプロイ方法 |
| 設定 | 環境変数、必須・任意、未設定時の既定値 |
| 保存先 | 入力・状態・結果の場所、命名規則、保持・削除の扱い |
| 認証 | API・Queue・Storageの認証方法と必要な権限 |
| 制限 | 容量、ページ数、処理時間、同時実行、変換品質など |
| テスト | 実行コマンド、必要な外部サービス、確認できる範囲 |

同期・非同期の違いや、再試行で結果がどう扱われるかも、対応する機能では説明します。
ローカルでの確認とAzure環境での確認は区別し、未検証の性能を保証しないでください。

## 実装と共通化

- 変換処理と、HTTP・Queue・Storageなどの接続処理を分けます。
- `ppt-pdf-to-images` の共通変換処理 `ConversionService` はAzureに依存させません。
- 同期HTTPとQueueで同じ変換動作を使う場合は、変換処理の重複を避けます。
- 他の変換にも共通する部分は、実際の利用例が出た範囲で共通化します。
- 将来の機能を想定しただけの共通基盤や、全機能共通の必須依存は増やしません。
- 既存のリクエストや設定の互換性を変える場合は、変更内容と移行方法を説明します。

現時点では、Maven Centralでの配布や、再利用用Javaライブラリ成果物は提供していません。
内部で共有するJavaクラスがあることと、公開ライブラリの提供は区別してください。

`ppt-pdf-to-images` の公開入口は `ConversionService` です。内部の `InputConverter` を通して、入力内容に対応する `PptConverter` または `PdfConverter` を選びます。`PageRendering` は寸法計算・画素数上限・Graphics設定を共有し、`ConversionService` は入出力・ページ数の上限、セマフォによる同時実行制御、画像エンコード、ZIP、ページごとのコールバックを担当します。

この機能に入力形式を追加するときは、`InputConverter` を実装して `ConversionService` に登録し、内容判定・描画・ページ指定・上限・失敗時のテストを追加します。HTTP・Queue・Blob・Playground・設定は同じ仕組みを利用し、1つのFunction AppとQueue `conversion-jobs` で提供します。

## 設定と秘密情報

- 接続文字列、キー、トークンなどをソース、README、テスト結果へ書き込みません。
- 実際の秘密情報を含む設定ファイルは `.gitignore` の対象にします。
- 設定のexampleには仮値を使い、実際の認証情報は含めません。
- 設定を追加したら、読み込み処理・example・READMEの名前と既定値を揃えます。
- 不要な秘密情報をURL、コマンド引数、ログに出さないようにします。

## 検証と変更の説明

- 変更した機能のビルドと、変更内容に対応するテストを実行します。
- 変換結果や外部サービスとの境界に影響する変更は、その振る舞いを確認します。
- Storageなどを使う統合テストは、専用のリソースやエミュレーターを使います。
- テストが作成したデータを片付ける際は、既存の利用者データを削除しません。
- 変更の説明には、変わる動作、確認した内容、未確認の事項を記載します。
- ビルド・テストと、Azureへのデプロイや外部への公開は別の操作として扱います。

## ライセンス

プロジェクトのMITライセンス本文と著作権表示を保持してください。
依存ライブラリ、ツール、フォント、サンプル資料には、それぞれのライセンスが適用されます。
追加・移動・再配布時には提供元の条件を確認し、必要なライセンス・NOTICE・出典表示を保持します。
外部の資料やテンプレートを、プロジェクトのMITライセンス対象として扱わないでください。
